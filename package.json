{
  "name": "arbitrum-bridge",
  "license": "MIT",
  "version": "0.0.2",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ arbitrum-bridge",
    "create-local": "graph create --node http://localhost:8020/ arbitrum-bridge",
    "remove-local": "graph remove --node http://localhost:8020/ arbitrum-bridge",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 arbitrum-bridge",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.33.0",
    "@graphprotocol/graph-ts": "0.27.0"
  },
  "devDependencies": { "matchstick-as": "0.5.0" }
}
